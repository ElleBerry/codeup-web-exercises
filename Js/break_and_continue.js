"use strict";
while (true){
var oddNumber = prompt("Please enter an odd number between 1 and 50.");
    if(oddNumber < 1 || oddNumber > 50 || oddNumber % 2 !== 0)break;
}

for (var i = 1; i <= 50; i++) {
    if (i === oddNUmber) {
        console.log("We are skipping this number: " + oddNumber);
    }else if (gitg)
}